#include "ws_server.h"
#include "esp_log.h"

static const char *TAG = "ws_server";

esp_err_t ws_server_start(void)
{
    ESP_LOGI(TAG, "Servidor WebSocket placeholder – aún no implementado.");
    return ESP_OK;
} 